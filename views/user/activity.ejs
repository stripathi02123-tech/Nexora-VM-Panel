<%- include('../partials/head', { title: 'Activity' }) %>
<%- include('../partials/ambient') %>
<%- include('../partials/topbar') %>
<%- include('../partials/sidebar') %>
<div class="main">
  <h2 style="font-size:22px" class="mb-3">My Activity</h2>
  <div class="card">
    <% if (logs.length === 0) { %><div class="empty"><p>No activity yet.</p></div><% } else { %>
    <div class="table-wrap">
      <table>
        <thead><tr><th>Event</th><th>Server</th><th>Details</th><th>IP</th><th>When</th></tr></thead>
        <tbody>
        <% for (const a of logs) { %>
          <tr>
            <td><span class="pill pill-blue"><%= a.event %></span></td>
            <td><% if (a.vm_id) { %><a href="/servers/<%= a.vm_id %>"><%= a.vm_name || ('#'+a.vm_id) %></a><% } else { %>-<% } %></td>
            <td class="muted truncate" style="max-width:320px"><%= a.details ? JSON.stringify(a.details) : '-' %></td>
            <td class="muted"><%= a.ip || '-' %></td>
            <td class="muted"><%= new Date(a.created_at).toLocaleString() %></td>
          </tr>
        <% } %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>
</div>
<%- include('../partials/music') %>
<%- include('../partials/foot') %>
